require "spec_helper"

describe Redpotion::Generators do
  it "has a version number" do
    expect(Redpotion::Generators::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
